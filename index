<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Book PC and console gaming sessions at Saguni Gaming Cafe.">
  <title>Saguni Gaming Cafe</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:ital,wght@0,400;0,600;0,700;0,800;1,700&family=Barlow+Condensed:wght@700;800;900&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #060608;
      --surface: #0e0e12;
      --surface2: #15151c;
      --gold: #f5c842;
      --gold2: #e8a820;
      --red: #e83232;
      --white: #f2f2f2;
      --muted: #8a8a9a;
      --line: rgba(255,255,255,0.08);
      --radius: 4px;
    }
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      background: var(--bg);
      color: var(--white);
      font-family: 'Barlow', sans-serif;
      font-size: 16px;
      overflow-x: hidden;
    }
    a { color: inherit; text-decoration: none; }
    button, input, select, textarea { font: inherit; }
    img { display: block; max-width: 100%; }

    /* ── TOPBAR ── */
    .topbar {
      background: #0a0a0e;
      border-bottom: 1px solid var(--line);
      padding: 0.5rem clamp(1rem,5vw,5rem);
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.82rem;
      color: var(--muted);
      gap: 1rem;
    }
    .topbar a { color: var(--muted); transition: color 0.2s; }
    .topbar a:hover { color: var(--gold); }
    .topbar-left, .topbar-right { display: flex; align-items: center; gap: 1.5rem; flex-wrap: wrap; }

    /* ── HEADER ── */
    .site-header {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(6,6,8,0.96);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--line);
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      padding: 0 clamp(1rem,5vw,5rem);
      height: 72px;
    }
    .logo {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .logo-icon {
      width: 44px;
      height: 44px;
      background: linear-gradient(135deg, var(--gold), var(--gold2));
      border-radius: var(--radius);
      display: grid;
      place-items: center;
      flex-shrink: 0;
    }
    .logo-icon svg { width: 26px; height: 26px; }
    .logo-text strong {
      display: block;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.35rem;
      letter-spacing: 2px;
      color: var(--white);
      line-height: 1;
    }
    .logo-text small {
      display: block;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--gold);
      line-height: 1;
      margin-top: 2px;
    }
    .nav { display: flex; align-items: center; gap: 2rem; }
    .nav a {
      font-weight: 700;
      font-size: 0.9rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      color: #ccc;
      transition: color 0.2s;
      position: relative;
    }
    .nav a::after {
      content: '';
      position: absolute;
      left: 0; bottom: -4px;
      width: 0; height: 2px;
      background: var(--gold);
      transition: width 0.2s;
    }
    .nav a:hover { color: var(--white); }
    .nav a:hover::after { width: 100%; }
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.65rem 1.4rem;
      border-radius: var(--radius);
      font-weight: 800;
      font-size: 0.88rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      border: none;
      cursor: pointer;
      transition: transform 0.15s, box-shadow 0.15s;
    }
    .btn:active { transform: scale(0.97); }
    .btn-gold {
      background: linear-gradient(135deg, var(--gold), var(--gold2));
      color: #0a0a0e;
    }
    .btn-gold:hover { box-shadow: 0 0 24px rgba(245,200,66,0.4); }
    .btn-outline {
      background: transparent;
      border: 2px solid var(--gold);
      color: var(--gold);
    }
    .btn-outline:hover { background: rgba(245,200,66,0.1); }
    .btn-red {
      background: linear-gradient(135deg, var(--red), #b02020);
      color: #fff;
    }
    .btn-red:hover { box-shadow: 0 0 24px rgba(232,50,50,0.4); }

    /* ── HERO SLIDER ── */
    .hero {
      position: relative;
      height: calc(100svh - 72px);
      min-height: 520px;
      overflow: hidden;
    }
    .slide {
      position: absolute;
      inset: 0;
      opacity: 0;
      transition: opacity 1s ease;
    }
    .slide.active { opacity: 1; }
    .slide img {
      width: 100%; height: 100%;
      object-fit: cover;
      filter: brightness(0.38) saturate(0.8);
    }
    .hero-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(90deg, rgba(6,6,8,0.92) 0%, rgba(6,6,8,0.5) 55%, rgba(6,6,8,0.15) 100%),
                  linear-gradient(0deg, rgba(6,6,8,0.85) 0%, transparent 40%);
    }
    .hero-content {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 0 clamp(1.5rem,8vw,8rem);
      max-width: 900px;
    }
    .hero-kicker {
      display: inline-block;
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: 0.8rem;
      letter-spacing: 5px;
      text-transform: uppercase;
      color: var(--gold);
      border: 1px solid rgba(245,200,66,0.35);
      padding: 0.3rem 0.8rem;
      border-radius: 2px;
      margin-bottom: 1.2rem;
      width: fit-content;
    }
    .hero-title {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(3.5rem, 9vw, 8rem);
      line-height: 0.92;
      letter-spacing: 2px;
      color: var(--white);
      margin-bottom: 1.2rem;
    }
    .hero-title span { color: var(--gold); }
    .hero-desc {
      font-size: clamp(1rem, 1.6vw, 1.2rem);
      font-weight: 600;
      color: rgba(242,242,242,0.75);
      max-width: 520px;
      line-height: 1.65;
      margin-bottom: 2rem;
    }
    .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; }
    .hero-dots {
      position: absolute;
      bottom: 2rem;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      gap: 0.5rem;
      z-index: 2;
    }
    .hero-dots button {
      width: 28px; height: 4px;
      border: none;
      background: rgba(255,255,255,0.25);
      cursor: pointer;
      border-radius: 2px;
      transition: background 0.3s, width 0.3s;
    }
    .hero-dots button.active {
      background: var(--gold);
      width: 48px;
    }

    /* ── GAME MARQUEE ── */
    .marquee-strip {
      background: var(--gold);
      padding: 0.6rem 0;
      overflow: hidden;
      white-space: nowrap;
    }
    .marquee-inner {
      display: inline-flex;
      gap: 0;
      animation: marquee 22s linear infinite;
    }
    .marquee-inner span {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1rem;
      letter-spacing: 3px;
      color: #0a0a0e;
      padding: 0 2rem;
    }
    .marquee-inner span.dot { color: #0a0a0e; padding: 0; }
    @keyframes marquee {
      from { transform: translateX(0); }
      to { transform: translateX(-50%); }
    }

    /* ── SECTIONS ── */
    section { padding: clamp(3rem,7vw,6rem) clamp(1.5rem,5vw,5rem); }
    .section-kicker {
      display: inline-block;
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: 0.78rem;
      letter-spacing: 5px;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 0.75rem;
    }
    h2 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(2.4rem,5vw,4.2rem);
      line-height: 0.95;
      letter-spacing: 1px;
      margin-bottom: 1rem;
    }
    h2 span { color: var(--gold); }
    h3 {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: 1.15rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }
    p { color: var(--muted); line-height: 1.7; }

    /* ── STATS BAR ── */
    .stats-bar {
      background: var(--surface);
      border-top: 1px solid var(--line);
      border-bottom: 1px solid var(--line);
      display: grid;
      grid-template-columns: repeat(4, 1fr);
    }
    .stat-item {
      padding: 2rem 1rem;
      text-align: center;
      border-right: 1px solid var(--line);
      position: relative;
    }
    .stat-item:last-child { border-right: none; }
    .stat-num {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(2.8rem,5vw,4rem);
      color: var(--gold);
      line-height: 1;
      display: block;
    }
    .stat-label {
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--muted);
      margin-top: 0.3rem;
    }

    /* ── ABOUT ── */
    .about-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: clamp(2rem,5vw,5rem);
      align-items: center;
    }
    .about-img {
      position: relative;
      border-radius: var(--radius);
      overflow: hidden;
    }
    .about-img img {
      width: 100%;
      aspect-ratio: 4/3;
      object-fit: cover;
      filter: saturate(0.8) contrast(1.1);
    }
    .about-img::before {
      content: '';
      position: absolute;
      inset: 0;
      border: 2px solid rgba(245,200,66,0.3);
      border-radius: var(--radius);
      z-index: 1;
      pointer-events: none;
    }
    .about-img::after {
      content: 'SAGUNI GAMING';
      position: absolute;
      bottom: 1rem; left: 1rem;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 0.9rem;
      letter-spacing: 4px;
      color: var(--gold);
      z-index: 2;
      background: rgba(6,6,8,0.75);
      padding: 0.3rem 0.6rem;
    }
    .about-content p { margin-bottom: 1.2rem; }
    .about-features {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0.75rem;
      margin-top: 1.5rem;
    }
    .about-feat {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      font-size: 0.88rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--white);
    }
    .about-feat::before {
      content: '';
      width: 8px; height: 8px;
      background: var(--gold);
      border-radius: 50%;
      flex-shrink: 0;
    }

    /* ── STATIONS ── */
    .stations-bg {
      background: var(--surface);
      border-top: 1px solid var(--line);
      border-bottom: 1px solid var(--line);
    }
    .station-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin-top: 2rem;
    }
    .station-card {
      background: var(--bg);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      overflow: hidden;
      transition: transform 0.2s, border-color 0.2s;
      position: relative;
    }
    .station-card:hover {
      transform: translateY(-6px);
      border-color: rgba(245,200,66,0.5);
    }
    .station-card img {
      width: 100%;
      aspect-ratio: 16/10;
      object-fit: cover;
      filter: saturate(0.85);
    }
    .station-tag {
      position: absolute;
      top: 1rem; left: 1rem;
      background: var(--gold);
      color: #0a0a0e;
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: 0.72rem;
      letter-spacing: 3px;
      text-transform: uppercase;
      padding: 0.2rem 0.6rem;
      border-radius: 2px;
    }
    .station-body { padding: 1.25rem; }
    .station-price {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.5rem;
      color: var(--gold);
      letter-spacing: 1px;
      margin-top: 0.5rem;
      display: block;
    }

    /* ── BOOKING ── */
    .booking-wrap {
      display: grid;
      grid-template-columns: 1fr 1.4fr;
      gap: clamp(2rem,5vw,5rem);
      align-items: start;
    }
    .booking-info h2 { margin-bottom: 1rem; }
    .booking-info p { margin-bottom: 1rem; }
    .info-list { list-style: none; margin-top: 1.5rem; }
    .info-list li {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      padding: 0.9rem 0;
      border-bottom: 1px solid var(--line);
      font-weight: 600;
      font-size: 0.95rem;
      color: var(--white);
    }
    .info-list li::before {
      content: '▸';
      color: var(--gold);
      flex-shrink: 0;
      margin-top: 1px;
    }
    .booking-form-box {
      background: var(--surface);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      padding: clamp(1.25rem,3vw,2rem);
    }
    .booking-form-box h3 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.8rem;
      letter-spacing: 2px;
      color: var(--white);
      margin-bottom: 1.25rem;
    }
    .form-row { display: grid; gap: 0.85rem; margin-bottom: 0.85rem; }
    .two-col { grid-template-columns: 1fr 1fr; }
    .three-col { grid-template-columns: 1fr 1fr 1fr; }
    label {
      display: grid;
      gap: 0.4rem;
      font-size: 0.78rem;
      font-weight: 800;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--muted);
    }
    input, select, textarea {
      width: 100%;
      background: rgba(0,0,0,0.5);
      border: 1px solid rgba(255,255,255,0.12);
      color: var(--white);
      border-radius: var(--radius);
      padding: 0.75rem 0.9rem;
      outline: none;
      transition: border-color 0.2s, box-shadow 0.2s;
    }
    input:focus, select:focus, textarea:focus {
      border-color: var(--gold);
      box-shadow: 0 0 0 3px rgba(245,200,66,0.1);
    }
    select option { background: #111; }
    .total-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: rgba(245,200,66,0.07);
      border: 1px solid rgba(245,200,66,0.2);
      border-radius: var(--radius);
      padding: 0.9rem 1rem;
      margin: 1rem 0;
    }
    .total-row span { font-size: 0.78rem; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; color: var(--muted); }
    .total-row strong { font-family: 'Bebas Neue', sans-serif; font-size: 1.8rem; color: var(--gold); letter-spacing: 1px; }
    .submit-btn {
      width: 100%;
      padding: 1rem;
      background: linear-gradient(135deg, var(--gold), var(--gold2));
      color: #0a0a0e;
      border: none;
      border-radius: var(--radius);
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.3rem;
      letter-spacing: 3px;
      cursor: pointer;
      transition: box-shadow 0.2s, transform 0.15s;
    }
    .submit-btn:hover { box-shadow: 0 0 28px rgba(245,200,66,0.45); }
    .submit-btn:active { transform: scale(0.98); }
    .submit-btn:disabled { opacity: 0.6; cursor: wait; }

    /* ── SAVED BOOKINGS ── */
    .bookings-section { background: var(--surface); border-top: 1px solid var(--line); }
    .booking-list { display: grid; gap: 0.85rem; margin-top: 1.5rem; }
    .empty-state {
      padding: 1.5rem;
      border: 1px dashed var(--line);
      border-radius: var(--radius);
      color: var(--muted);
      text-align: center;
      font-size: 0.9rem;
    }
    .saved-booking {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 1rem;
      align-items: center;
      padding: 1.1rem 1.25rem;
      background: var(--bg);
      border: 1px solid var(--line);
      border-radius: var(--radius);
    }
    .saved-booking strong { display: block; font-weight: 800; font-size: 0.95rem; color: var(--gold); }
    .saved-booking small { display: block; color: var(--muted); font-size: 0.82rem; margin-top: 0.25rem; line-height: 1.6; }
    .booking-actions { display: flex; gap: 0.5rem; }
    .booking-actions button {
      padding: 0.5rem 0.9rem;
      border: 1px solid var(--line);
      background: rgba(0,0,0,0.5);
      color: var(--white);
      border-radius: var(--radius);
      font-size: 0.8rem;
      font-weight: 700;
      cursor: pointer;
      transition: border-color 0.2s;
    }
    .booking-actions button:hover { border-color: var(--gold); color: var(--gold); }

    /* ── TESTIMONIALS ── */
    .testimonials-bg {
      background: var(--surface2);
      border-top: 1px solid var(--line);
      border-bottom: 1px solid var(--line);
    }
    .testi-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin-top: 2rem;
    }
    .testi-card {
      background: var(--surface);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      padding: 1.75rem;
      position: relative;
    }
    .testi-quote {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 5rem;
      line-height: 0.6;
      color: var(--gold);
      opacity: 0.25;
      position: absolute;
      top: 1.25rem; left: 1.25rem;
    }
    .testi-text {
      color: rgba(242,242,242,0.8);
      font-size: 0.92rem;
      line-height: 1.75;
      margin-bottom: 1.25rem;
      position: relative;
      z-index: 1;
    }
    .testi-author { font-weight: 800; font-size: 0.88rem; color: var(--gold); letter-spacing: 1px; }
    .testi-role { font-size: 0.78rem; color: var(--muted); margin-top: 2px; }
    .stars { color: var(--gold); font-size: 0.8rem; letter-spacing: 2px; margin-bottom: 0.75rem; }

    /* ── VISIT ── */
    .visit-grid {
      display: grid;
      grid-template-columns: 1.2fr 1fr;
      gap: clamp(2rem,5vw,5rem);
      align-items: start;
    }
    .visit-map-placeholder {
      background: var(--surface);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      aspect-ratio: 4/3;
      display: grid;
      place-items: center;
      color: var(--muted);
      font-size: 0.85rem;
      text-align: center;
      gap: 0.5rem;
    }
    .contact-cards { display: grid; gap: 0.85rem; margin-top: 1.5rem; }
    .contact-card {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1rem 1.25rem;
      background: var(--surface);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      transition: border-color 0.2s;
    }
    .contact-card:hover { border-color: rgba(245,200,66,0.4); }
    .contact-icon {
      width: 42px; height: 42px;
      background: rgba(245,200,66,0.1);
      border: 1px solid rgba(245,200,66,0.25);
      border-radius: var(--radius);
      display: grid;
      place-items: center;
      flex-shrink: 0;
      color: var(--gold);
      font-size: 1.1rem;
    }
    .contact-label { font-size: 0.7rem; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; color: var(--muted); }
    .contact-val { font-weight: 700; font-size: 0.95rem; color: var(--white); margin-top: 2px; }

    /* ── FOOTER ── */
    .site-footer {
      background: #030304;
      border-top: 1px solid var(--line);
      padding: 2rem clamp(1.5rem,5vw,5rem);
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 1rem;
      flex-wrap: wrap;
    }
    .footer-copy { font-size: 0.82rem; color: var(--muted); }
    .footer-links { display: flex; gap: 1.5rem; }
    .footer-links a { font-size: 0.82rem; color: var(--muted); transition: color 0.2s; }
    .footer-links a:hover { color: var(--gold); }

    /* ── TOAST ── */
    .toast {
      position: fixed;
      bottom: 1.5rem; right: 1.5rem;
      z-index: 999;
      background: var(--surface);
      border: 1px solid rgba(245,200,66,0.5);
      border-radius: var(--radius);
      padding: 1rem 1.25rem;
      font-size: 0.9rem;
      font-weight: 700;
      color: var(--white);
      max-width: min(22rem, calc(100vw - 3rem));
      box-shadow: 0 16px 48px rgba(0,0,0,0.6);
      transform: translateY(1rem);
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.2s, transform 0.2s;
    }
    .toast.show { opacity: 1; transform: translateY(0); pointer-events: auto; }

    /* ── SCROLL ANIMATIONS ── */
    .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.6s ease, transform 0.6s ease;
    }
    .reveal.visible { opacity: 1; transform: translateY(0); }
    .reveal-delay-1 { transition-delay: 0.1s; }
    .reveal-delay-2 { transition-delay: 0.2s; }
    .reveal-delay-3 { transition-delay: 0.3s; }

    /* ── RESPONSIVE ── */
    @media (max-width: 1024px) {
      .stats-bar { grid-template-columns: repeat(2, 1fr); }
      .stat-item:nth-child(2) { border-right: none; }
      .station-grid { grid-template-columns: 1fr 1fr; }
      .testi-grid { grid-template-columns: 1fr 1fr; }
      .testi-grid .testi-card:last-child { display: none; }
    }
    @media (max-width: 768px) {
      .nav { display: none; }
      .topbar { display: none; }
      .about-grid, .booking-wrap, .visit-grid { grid-template-columns: 1fr; }
      .station-grid { grid-template-columns: 1fr; }
      .testi-grid { grid-template-columns: 1fr; }
      .testi-grid .testi-card:last-child { display: block; }
      .two-col, .three-col { grid-template-columns: 1fr; }
      .saved-booking { grid-template-columns: 1fr; }
      .booking-actions { width: 100%; }
      .booking-actions button { flex: 1; }
      .site-footer { flex-direction: column; text-align: center; }
    }
    @media (max-width: 480px) {
      .stats-bar { grid-template-columns: 1fr 1fr; }
    }
  </style>
</head>
<body>

  <!-- TOPBAR -->
  <div class="topbar">
    <div class="topbar-left">
      <a href="tel:+910000000000">📞 +91 00000 00000</a>
      <a href="mailto:bookings@sagunigaming.in">✉ bookings@sagunigaming.in</a>
    </div>
    <div class="topbar-right">
      <a href="https://instagram.com" target="_blank" rel="noreferrer">Instagram</a>
    </div>
  </div>

  <!-- HEADER -->
  <header class="site-header">
    <a class="logo" href="#top">
      <div class="logo-icon">
        <svg viewBox="0 0 26 26" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M13 2 L4 8 L4 18 L13 24 L22 18 L22 8 Z" fill="#0a0a0e" stroke="#0a0a0e" stroke-width="1"/>
          <path d="M15 4 L7 14 H12 L10 22 L19 12 H14 Z" fill="#0a0a0e"/>
        </svg>
      </div>
      <div class="logo-text">
        <strong>Saguni Gaming</strong>
        <small>Premium Cafe</small>
      </div>
    </a>
    <nav class="nav">
      <a href="#about">About</a>
      <a href="#stations">Stations</a>
      <a href="#book">Book</a>
      <a href="#visit">Visit</a>
    </nav>
    <a class="btn btn-gold" href="#book">Book a Slot</a>
  </header>

  <main id="top">

    <!-- HERO -->
    <section class="hero" aria-label="Hero">
      <div class="slide active" id="slide0">
        <img src="https://images.unsplash.com/photo-1560253023-3ec5d502959f?auto=format&fit=crop&w=1800&q=85" alt="Gaming cafe setup">
      </div>
      <div class="slide" id="slide1">
        <img src="https://images.unsplash.com/photo-1598550476439-6847785fcea6?auto=format&fit=crop&w=1800&q=85" alt="PC gaming setup">
      </div>
      <div class="slide" id="slide2">
        <img src="https://images.unsplash.com/photo-1511512578047-dfb367046420?auto=format&fit=crop&w=1800&q=85" alt="Gaming keyboard">
      </div>
      <div class="hero-overlay"></div>
      <div class="hero-content">
        <p class="hero-kicker">Welcome to Saguni Gaming Cafe</p>
        <h1 class="hero-title">Mumbai's<br><span>Premier</span><br>Gaming Hub</h1>
        <p class="hero-desc">For serious sessions, squad nights, and quick rematches. Pick your setup, lock your slot, walk in ready.</p>
        <div class="hero-actions">
          <a class="btn btn-gold" href="#book">Book a Slot</a>
          <a class="btn btn-outline" href="#stations">View Setups</a>
        </div>
      </div>
      <div class="hero-dots" id="heroDots"></div>
    </section>

    <!-- MARQUEE -->
    <div class="marquee-strip" aria-hidden="true">
      <div class="marquee-inner" id="marqueeInner"></div>
    </div>

    <!-- STATS -->
    <div class="stats-bar">
      <div class="stat-item reveal">
        <span class="stat-num" data-target="15">0</span>
        <div class="stat-label">Gaming PCs</div>
      </div>
      <div class="stat-item reveal reveal-delay-1">
        <span class="stat-num" data-target="4">0</span>
        <div class="stat-label">PS5 Bays</div>
      </div>
      <div class="stat-item reveal reveal-delay-2">
        <span class="stat-num" data-target="30">0</span>
        <div class="stat-label">Daily Players</div>
      </div>
      <div class="stat-item reveal reveal-delay-3">
        <span class="stat-num" data-target="50">0</span>
        <div class="stat-label">Games Available</div>
      </div>
    </div>

    <!-- ABOUT -->
    <section id="about">
      <div class="about-grid">
        <div class="about-img reveal">
          <img src="https://images.unsplash.com/photo-1542751371-adc38448a05e?auto=format&fit=crop&w=900&q=80" alt="Gaming cafe interior">
        </div>
        <div class="about-content reveal reveal-delay-1">
          <p class="section-kicker">Welcome to Saguni</p>
          <h2>Ultimate Gaming<br><span>Heaven</span></h2>
          <p>Welcome to Saguni Gaming Cafe — the ultimate gaming destination. Whether you're a casual player or a competitive gamer, we've built a space designed to immerse you in the world of high-performance gaming.</p>
          <p>Our state-of-the-art setups, great ambiance, and passionate community make us the go-to destination for gamers of all levels.</p>
          <div class="about-features">
            <div class="about-feat">High-FPS Gaming PCs</div>
            <div class="about-feat">PS5 Consoles</div>
            <div class="about-feat">Squad Blocks</div>
            <div class="about-feat">Fast Booking</div>
            <div class="about-feat">Tournaments</div>
            <div class="about-feat">Great Vibes</div>
          </div>
          <div style="margin-top:2rem;">
            <a class="btn btn-gold" href="#book">Book Now</a>
          </div>
        </div>
      </div>
    </section>

    <!-- STATIONS -->
    <section class="stations-bg" id="stations">
      <p class="section-kicker reveal">Pick Your Lane</p>
      <h2 class="reveal">Setups That Match<br><span>The Session</span></h2>
      <div class="station-grid">
        <article class="station-card reveal">
          <img src="https://images.unsplash.com/photo-1598550476439-6847785fcea6?auto=format&fit=crop&w=900&q=80" alt="Gaming PC">
          <span class="station-tag">Aim Sharp</span>
          <div class="station-body">
            <h3>Gaming PC</h3>
            <p>High FPS station with keyboard, mouse, headset, and multiplayer titles ready to go.</p>
            <span class="station-price">Rs 120 / hr / player</span>
          </div>
        </article>
        <article class="station-card reveal reveal-delay-1">
          <img src="https://images.unsplash.com/photo-1606144042614-b2417e99c4e3?auto=format&fit=crop&w=900&q=80" alt="PS5 Bay">
          <span class="station-tag">Settle The Score</span>
          <div class="station-body">
            <h3>PS5 Bay</h3>
            <p>Console bay for FIFA, Tekken, WWE, co-op story games, and chill sessions.</p>
            <span class="station-price">Rs 100 solo · Rs 180 duo</span>
          </div>
        </article>
        <article class="station-card reveal reveal-delay-2">
          <img src="https://images.unsplash.com/photo-1511512578047-dfb367046420?auto=format&fit=crop&w=900&q=80" alt="Squad Block">
          <span class="station-tag">Full Send</span>
          <div class="station-body">
            <h3>Squad Block</h3>
            <p>Book multiple players for birthdays, LAN nights, and tournament warmups.</p>
            <span class="station-price">From Rs 450 / hr</span>
          </div>
        </article>
      </div>
    </section>

    <!-- BOOKING -->
    <section id="book">
      <div class="booking-wrap">
        <div class="booking-info reveal">
          <p class="section-kicker">Book Online</p>
          <h2>Your Next Session<br><span>Starts Here</span></h2>
          <p>Choose your station, game, date, time, and player count. Your booking ID is saved instantly.</p>
          <ul class="info-list">
            <li>No waiting — lock your slot before you arrive</li>
            <li>Real-time estimate calculated for you</li>
            <li>Booking ID saved in your browser</li>
            <li>Syncs to Firebase when configured</li>
          </ul>
        </div>
        <div class="reveal reveal-delay-1">
          <div class="booking-form-box">
            <h3>Reserve Your Slot</h3>
            <form id="bookingForm">
              <div class="form-row two-col">
                <label>Name<input id="name" name="name" type="text" placeholder="Your name" required></label>
                <label>Phone<input id="phone" name="phone" type="tel" placeholder="10-digit mobile" pattern="[0-9 +()-]{8,16}" required></label>
              </div>
              <div class="form-row two-col">
                <label>
                  Setup
                  <select id="setup" name="setup" required>
                    <option value="Gaming PC" data-rate="120" data-pricing="per-player">Gaming PC – Rs 120/hr/player</option>
                    <option value="PS5 Bay" data-solo-rate="100" data-duo-rate="180" data-pricing="ps5">PS5 Bay – Rs 100 solo / Rs 180 duo</option>
                    <option value="Squad Block" data-rate="450" data-pricing="flat">Squad Block – Rs 450/hr</option>
                  </select>
                </label>
                <label>
                  Game
                  <select id="game" name="game" required>
                    <option>Valorant</option>
                    <option>GTA V</option>
                    <option>FIFA / FC</option>
                    <option>Tekken</option>
                    <option>Call of Duty</option>
                    <option>CS2</option>
                    <option>WWE 2K</option>
                    <option>Other / decide there</option>
                  </select>
                </label>
              </div>
              <div class="form-row three-col">
                <label>Date<input id="date" name="date" type="date" required></label>
                <label>Start Time<input id="time" name="time" type="time" min="10:00" max="23:30" required></label>
                <label>Hours<input id="hours" name="hours" type="number" min="1" max="8" value="1" required></label>
              </div>
              <div class="form-row two-col">
                <label>Players<input id="players" name="players" type="number" min="1" max="8" value="1" required></label>
                <label>Notes<input id="notes" name="notes" type="text" placeholder="Preferred seat or game"></label>
              </div>
              <div class="total-row">
                <span>Estimated Total</span>
                <strong id="total">Rs 120</strong>
              </div>
              <button class="submit-btn" type="submit">Confirm Booking</button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <!-- SAVED BOOKINGS -->
    <section class="bookings-section" aria-label="Saved bookings">
      <p class="section-kicker reveal">Your Slots</p>
      <h2 class="reveal">Saved <span>Bookings</span></h2>
      <div class="booking-list" id="bookingList"></div>
    </section>

    <!-- TESTIMONIALS -->
    <section class="testimonials-bg">
      <p class="section-kicker reveal" style="text-align:center">Happy Players</p>
      <h2 class="reveal" style="text-align:center">What Gamers <span>Say</span></h2>
      <div class="testi-grid">
        <div class="testi-card reveal">
          <div class="testi-quote">"</div>
          <div class="stars">★★★★★</div>
          <p class="testi-text">Visited with my friends and had an amazing time! The PCs are top-notch and the staff is super friendly. Best gaming cafe in the area by far.</p>
          <div class="testi-author">Rahul M.</div>
          <div class="testi-role">Regular Player</div>
        </div>
        <div class="testi-card reveal reveal-delay-1">
          <div class="testi-quote">"</div>
          <div class="stars">★★★★★</div>
          <p class="testi-text">Saguni is the real deal. High FPS setups, fast internet, and the PS5 bay is always ready for FIFA grudge matches. Won't game anywhere else.</p>
          <div class="testi-author">Vishal K.</div>
          <div class="testi-role">Competitive Gamer</div>
        </div>
        <div class="testi-card reveal reveal-delay-2">
          <div class="testi-quote">"</div>
          <div class="stars">★★★★☆</div>
          <p class="testi-text">Booked a Squad Block for my birthday and it was epic. Easy booking, great setup, great vibes. Highly recommend for group sessions!</p>
          <div class="testi-author">Priya S.</div>
          <div class="testi-role">First-time Visitor</div>
        </div>
      </div>
    </section>

    <!-- VISIT -->
    <section id="visit">
      <div class="visit-grid">
        <div class="reveal">
          <p class="section-kicker">Find Us</p>
          <h2>Pull Up.<br><span>Power On.</span> Play.</h2>
          <p style="margin-bottom:1.5rem">Come visit us. Replace these placeholders with your actual address and contact details before going live.</p>
          <div class="contact-cards">
            <a class="contact-card" href="tel:+910000000000">
              <div class="contact-icon">📞</div>
              <div>
                <div class="contact-label">Phone</div>
                <div class="contact-val">+91 00000 00000</div>
              </div>
            </a>
            <a class="contact-card" href="https://maps.google.com" target="_blank" rel="noreferrer">
              <div class="contact-icon">📍</div>
              <div>
                <div class="contact-label">Address</div>
                <div class="contact-val">Add your cafe address here</div>
              </div>
            </a>
            <a class="contact-card" href="mailto:bookings@sagunigaming.in">
              <div class="contact-icon">✉</div>
              <div>
                <div class="contact-label">Email</div>
                <div class="contact-val">bookings@sagunigaming.in</div>
              </div>
            </a>
          </div>
        </div>
        <div class="reveal reveal-delay-1">
          <div class="visit-map-placeholder">
            <div>
              <div style="font-size:2.5rem;margin-bottom:0.5rem">🗺</div>
              <div style="font-weight:700;color:var(--white);margin-bottom:0.25rem">Map Placeholder</div>
              <div>Embed a Google Maps iframe here</div>
            </div>
          </div>
        </div>
      </div>
    </section>

  </main>

  <!-- TOAST -->
  <div class="toast" id="toast" role="status" aria-live="polite"></div>

  <!-- FOOTER -->
  <footer class="site-footer">
    <div class="footer-copy">© 2025 Saguni Gaming Cafe. All rights reserved.</div>
    <div class="footer-links">
      <a href="#about">About</a>
      <a href="#stations">Stations</a>
      <a href="#book">Book</a>
      <a href="#visit">Visit</a>
    </div>
  </footer>

  <script type="module">
    /* ── FIREBASE (inlined config) ── */
    const storageKey = "saguni-bookings";
    let firestore = null;
    let firebaseReady = false;
    const firebaseCollectionName = "saguniBookings";

    async function setupFirebase() {
      try {
        const firebaseConfig = {
          apiKey: "AIzaSyCu3vD_tRWnH-ybL3ivd594Pj1rNZs7VJ8",
          authDomain: "sagunigaming-75752.firebaseapp.com",
          projectId: "sagunigaming-75752",
          storageBucket: "sagunigaming-75752.firebasestorage.app",
          messagingSenderId: "537941448957",
          appId: "1:537941448957:web:ed790048aeef6867340694"
        };
        const hasConfig = Object.values(firebaseConfig).every(Boolean);
        if (!hasConfig) return;
        const [firebaseApp, firestoreModule] = await Promise.all([
          import("https://www.gstatic.com/firebasejs/10.12.5/firebase-app.js"),
          import("https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js")
        ]);
        const app = firebaseApp.initializeApp(firebaseConfig);
        firestore = {
          db: firestoreModule.getFirestore(app),
          addDoc: firestoreModule.addDoc,
          collection: firestoreModule.collection,
          serverTimestamp: firestoreModule.serverTimestamp
        };
        firebaseReady = true;
      } catch (e) {
        console.warn("Firebase not connected.", e);
      }
    }

    /* ── HERO SLIDER ── */
    const slides = document.querySelectorAll(".slide");
    const dotsContainer = document.getElementById("heroDots");
    let current = 0;
    slides.forEach((_, i) => {
      const btn = document.createElement("button");
      btn.setAttribute("aria-label", `Slide ${i + 1}`);
      if (i === 0) btn.classList.add("active");
      btn.addEventListener("click", () => goTo(i));
      dotsContainer.appendChild(btn);
    });
    function goTo(idx) {
      slides[current].classList.remove("active");
      dotsContainer.children[current].classList.remove("active");
      current = idx;
      slides[current].classList.add("active");
      dotsContainer.children[current].classList.add("active");
    }
    setInterval(() => goTo((current + 1) % slides.length), 5000);

    /* ── MARQUEE ── */
    const games = ["Valorant","GTA V","FIFA FC","Tekken","Call of Duty","CS2","WWE 2K","Mortal Kombat","Dota 2","PUBG","Red Dead Redemption 2","Minecraft"];
    const inner = document.getElementById("marqueeInner");
    const repeated = [...games, ...games];
    inner.innerHTML = repeated.map(g => `<span>${g}</span><span class="dot">◆</span>`).join("");

    /* ── SCROLL REVEAL ── */
    const revealEls = document.querySelectorAll(".reveal");
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add("visible"); observer.unobserve(e.target); } });
    }, { threshold: 0.12 });
    revealEls.forEach(el => observer.observe(el));

    /* ── COUNTER ANIMATION ── */
    const counters = document.querySelectorAll(".stat-num[data-target]");
    const counterObs = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (!e.isIntersecting) return;
        const el = e.target;
        const target = +el.dataset.target;
        let count = 0;
        const step = Math.ceil(target / 40);
        const tick = setInterval(() => {
          count = Math.min(count + step, target);
          el.textContent = count + "+";
          if (count >= target) clearInterval(tick);
        }, 35);
        counterObs.unobserve(el);
      });
    }, { threshold: 0.5 });
    counters.forEach(c => counterObs.observe(c));

    /* ── BOOKING FORM ── */
    const form = document.getElementById("bookingForm");
    const setupEl = document.getElementById("setup");
    const hoursEl = document.getElementById("hours");
    const playersEl = document.getElementById("players");
    const totalEl = document.getElementById("total");
    const dateInput = document.getElementById("date");
    const timeInput = document.getElementById("time");
    const bookingList = document.getElementById("bookingList");
    const toastEl = document.getElementById("toast");

    function todayValue() { return new Date().toISOString().slice(0, 10); }

    function setDefaultSlot() {
      const slot = new Date();
      slot.setMinutes(slot.getMinutes() < 30 ? 30 : 60, 0, 0);
      if (slot.getHours() < 10) slot.setHours(10, 0, 0, 0);
      if (slot.getHours() > 23 || (slot.getHours() === 23 && slot.getMinutes() > 30)) {
        slot.setDate(slot.getDate() + 1); slot.setHours(10, 0, 0, 0);
      }
      dateInput.value = slot.toISOString().slice(0, 10);
      timeInput.value = `${String(slot.getHours()).padStart(2,"0")}:${String(slot.getMinutes()).padStart(2,"0")}`;
    }

    function getPricing() { return setupEl.selectedOptions[0].dataset; }
    function fmtCurrency(v) { return `Rs ${Number(v).toLocaleString("en-IN")}`; }

    function getEstimate() {
      const p = getPricing();
      const h = Math.max(1, Number(hoursEl.value || 1));
      const pl = Math.max(1, Number(playersEl.value || 1));
      if (p.pricing === "ps5") return (pl === 1 ? Number(p.soloRate) : Number(p.duoRate)) * h;
      if (p.pricing === "flat") return Number(p.rate) * h;
      return Number(p.rate) * h * pl;
    }

    function updateTotal() { totalEl.textContent = fmtCurrency(getEstimate()); }

    function updatePlayerLimit() {
      if (getPricing().pricing === "ps5") {
        playersEl.max = "2";
        if (Number(playersEl.value) > 2) playersEl.value = "2";
      } else { playersEl.max = "8"; }
      updateTotal();
    }

    function readBookings() { return JSON.parse(localStorage.getItem(storageKey) || "[]"); }
    function writeBookings(b) { localStorage.setItem(storageKey, JSON.stringify(b)); }

    function showToast(msg) {
      toastEl.textContent = msg;
      toastEl.classList.add("show");
      setTimeout(() => toastEl.classList.remove("show"), 3200);
    }

    function escHtml(v) {
      return String(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
    }

    function bookingCode() { return `SG-${Math.random().toString(36).slice(2,6).toUpperCase()}-${Date.now().toString().slice(-4)}`; }

    function renderBookings() {
      const bookings = readBookings();
      if (!bookings.length) {
        bookingList.innerHTML = '<div class="empty-state">No saved bookings yet. Confirm a slot above and it will appear here.</div>';
        return;
      }
      bookingList.innerHTML = bookings.map(b => `
        <article class="saved-booking">
          <div>
            <strong>${escHtml(b.code)} — ${escHtml(b.setup)}</strong>
            <small>${escHtml(b.date)} at ${escHtml(b.time)} · ${escHtml(b.hours)}hr · ${escHtml(b.players)} player(s) · ${escHtml(b.game)}</small>
            <small>${escHtml(b.name)} · ${escHtml(b.phone)} · ${escHtml(fmtCurrency(b.estimate))}${b.synced ? " · Firebase synced ✓" : ""}</small>
          </div>
          <div class="booking-actions">
            <button type="button" data-copy="${escHtml(b.code)}">Copy ID</button>
            <button type="button" data-delete="${escHtml(b.code)}">Cancel</button>
          </div>
        </article>`).join("");
    }

    function collectBooking() {
      const d = new FormData(form);
      return {
        code: bookingCode(),
        name: d.get("name").trim(), phone: d.get("phone").trim(),
        setup: d.get("setup"), game: d.get("game"),
        date: d.get("date"), time: d.get("time"),
        hours: Number(d.get("hours")), players: Number(d.get("players")),
        notes: d.get("notes").trim(),
        estimate: getEstimate(), synced: false,
        createdAt: new Date().toISOString()
      };
    }

    async function saveOnline(booking) {
      if (!firebaseReady || !firestore) return false;
      await firestore.addDoc(firestore.collection(firestore.db, firebaseCollectionName), {
        ...booking, firebaseCreatedAt: firestore.serverTimestamp()
      });
      return true;
    }

    function isPast(dv, tv) { return new Date(`${dv}T${tv}`) < new Date(); }

    dateInput.min = todayValue();
    setDefaultSlot();

    ["change","input"].forEach(ev => {
      setupEl.addEventListener(ev, updatePlayerLimit);
      hoursEl.addEventListener(ev, updateTotal);
      playersEl.addEventListener(ev, updateTotal);
    });

    form.addEventListener("submit", async (e) => {
      e.preventDefault();
      if (isPast(dateInput.value, timeInput.value)) { showToast("Pick a future date and time."); return; }
      const booking = collectBooking();
      const btn = form.querySelector(".submit-btn");
      btn.disabled = true; btn.textContent = "Saving…";
      try { booking.synced = await saveOnline(booking); } catch (err) { console.warn(err); }
      writeBookings([booking, ...readBookings()].slice(0, 8));
      renderBookings();
      showToast(booking.synced
        ? `Booked ${booking.setup} online. ID: ${booking.code}`
        : `Booked ${booking.setup} locally. ID: ${booking.code}`);
      form.reset(); setDefaultSlot(); updatePlayerLimit();
      btn.disabled = false; btn.textContent = "Confirm Booking";
    });

    bookingList.addEventListener("click", async (e) => {
      const cp = e.target.dataset.copy;
      const dl = e.target.dataset.delete;
      if (cp) { await navigator.clipboard.writeText(cp); showToast(`Copied ${cp}`); }
      if (dl) { writeBookings(readBookings().filter(b => b.code !== dl)); renderBookings(); showToast(`Cancelled ${dl}`); }
    });

    setupFirebase().then(() => { updatePlayerLimit(); renderBookings(); });
  </script>
</body>
</html>
